# Copy this file to `.env` (or set these as real environment variables) — never commit the real .env

DB_USER=staffdesk_user
DB_PASSWORD=changeme
JWT_SECRET=replace-with-a-long-random-string-at-least-256-bits
